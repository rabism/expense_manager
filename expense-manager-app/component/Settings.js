import React from "react";
import { Container, Text } from "native-base";

const Settings = (props) => (
  <Container>
    <Text> This is child settings page!! </Text>
  </Container>
);

export default Settings;
